Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.4,<5.0
python-dotenv>=1.0,<2.0
PyJWT[crypto]>=2.8,<3.0
psycopg[binary]>=3.1,<4.0
dj-database-url>=2.1,<3.0
requests>=2.0,<3.0
gunicorn>=21.0,<23.0
# WebSockets. Needs an ASGI server -- daphne, not gunicorn's WSGI worker.
# daphne rather than uvicorn because it is Channels' own server: it provides
# the ASGI runserver used locally and the test utilities import it.
channels>=4.0,<5.0
daphne>=4.0,<5.0
